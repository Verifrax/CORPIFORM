# CORPIFORM ENVIRONMENT EXAMPLE
# This file documents required environment variables.
# It must never be used directly in production.

# --- EXECUTION INPUTS ---
EXECUTION_COMMAND_PATH=/path/to/execution.command.json
AUTHORITY_SEAL_PATH=/path/to/authority.seal.json
EXECUTION_CUSTODIAN=corpiform-operator-id

# --- SYSTEM IDENTITY ---
SYSTEM_FINGERPRINT=corpiform-instance-fingerprint
BUILD_HASH=corpiform-build-hash
CORPIFORM_VERSION=v0.1.0

# --- SIGNING KEYS ---
RECEIPT_SIGNING_KEY=/path/to/receipt_private.key
DENIAL_SIGNING_KEY=/path/to/denial_private.key

# --- VERIFICATION KEYS ---
TRUSTED_PUBLIC_KEY=/path/to/trusted_public.key

# --- RUNTIME BEHAVIOR ---
TZ=UTC
LANG=C
LC_ALL=C

# --- SECURITY ---
# Secrets must be injected via secure runtime mechanisms only.
# Never commit real values.
