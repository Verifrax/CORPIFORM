# Change Control

This file defines the governance change discipline.

Minimum:
- policy IDs are immutable once published
- versioning uses semver for policy bundles
- emergency procedure must be explicit + logged
